# Bundle Demo

This folder demonstrates **folder-level peeps** — where an entire directory becomes an app.

When a peep matches a folder (instead of a single file), OpenPeep treats the whole folder as the data source. This enables project boards, dashboards, and multi-file editors.

The project board you see in OpenPeep's main view is itself a bundle peep!
